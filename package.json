{
  "name": "calendar-app",
  "private": true,
  "version": "1.0.0",
  "description": "行事曆 App：網頁版（GitHub Pages）＋ Android（Capacitor）",
  "scripts": {
    "www": "node scripts/build-www.mjs",
    "sync": "node scripts/build-www.mjs && cap sync android",
    "apk": "node scripts/build-www.mjs && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.2",
    "@capacitor/local-notifications": "^8.3.1",
    "@supabase/supabase-js": "^2.117.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2",
    "esbuild": "^0.28.2"
  }
}